// NOTE: The Enum data registered in the @libs/model in the client will cause an error.
// NOTE: Copy and import the Enum data here when using it.

export enum Role {
  USER = "USER",
  ADMIN = "ADMIN",
}

export enum Status {
  ACTIVE = "ACTIVE",
  INACTIVE = "INACTIVE",
  DELETE = "DELETE",
}
